# ui/

Placeholder for the **Phase 2.5** first-class front-end: a galaxy/map view that
drills into per-system top-down views, with system switching, zoom, layer
toggles, junction queue/ETA panels, and time controls — consuming the engine API
([`contracts/engine-openapi.yaml`](../contracts/engine-openapi.yaml)).

The Phase 1 Sol map (vanilla Canvas, served by the engine at `GET /`) is the
proof-of-concept this generalizes. Not built yet.
