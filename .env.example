# Copy to .env and adjust for your machine. Loaded by the justfile.
# .env is gitignored; this example is committed.

# Deploy target (ssh host) and published port for `just deploy` / `health` / etc.
HVSIM_HOST=kubsdb
HVSIM_PORT=4667
